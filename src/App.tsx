import React from 'react';
import { Box, Typography } from '@mui/material';

export default function App() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Email Builder</Typography>
    </Box>
  );
}